#include "tree.h"
#include <stdio.h>

// struct Node* tree(struct Node* n ) {
// 	struct Node* root = NULL;

// 	if( root == 0 ) {
// 		root = n;
// 	}else {
//         while( (root++) ) 
//         {
//             printf("Finding...");
//         }
//     }
// 	return root;
// }